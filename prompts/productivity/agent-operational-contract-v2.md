---
key: agent-operational-contract-v2
title: Операционный контракт агента v2 — manual, домен, контекст
patterns: [source-sync]
tags: [docs-as-code, workflow, agent-contract, bdd-verification, three-layer-docs, migration-plan]
use_case: Скармливается новому AI-агенту как предложение развернуть проектную документацию из трёх слоёв (manual — для человека, entity+behavior — общая правда с BDD-верификацией, context+conventions — для агента). Если документации ещё нет — собрать с нуля. Если есть — выдать план миграции. Минимизирует присутствие человека, агент сам держит consistency, ставит и снимает verified, обновляет entities и conventions, ищет противоречия. Человек нужен для ручных шагов из manuals и для явного согласования изменений.
---

# Операционный контракт агента v2 — manual, домен, контекст

## TL;DR

- **Четыре слоя:** manual (для человека), entity + behavior (общая
  правда, BDD-верификация), context (для агента). Ни один слой не
  дублирует другой.
- **`CHANGELOG.md` — журнал решений.** Крупные архитектурные решения,
  смена стека, изменения внешних зависимостей, крупные правки
  `context/`, ключевые вехи. Отвечает на «когда» и «почему», не на
  «как сейчас». Формат — одна запись = одна строка.
- **Пять сценариев:** `plan`, `execute`, `audit`, `challenge`, `report`.
  См. раздел «Сценарии взаимодействия» и `context/workflow.md`.
- **Пять жёстких правил:**
  1. Перед любым `plan` и любой правкой — сначала прочитай проектную
     документацию по релевантному namespace.
  2. `verified: true` живёт в `docs/behavior/*.yml`, ставится только по
     свежей проверке (`curl`, `grep`, тест, чтение кода) и сбрасывается
     только при семантическом изменении поведения.
  3. Все устойчивые уточнения — в репозиторий, не в memory / hooks
     (принцип открытости). Команда «запомни» = правка проектной
     документации по умолчанию.
  4. Сложная задача идёт атомарными фазами по типу коммита
     (`docs` → `fix` → `refactor` → `feat`). Смешивать типы в одной
     фазе нельзя.
  5. Крупные решения (архитектура, стек, внешние зависимости, крупные
     правки `context/`, вехи) фиксируются одной строкой в
     `CHANGELOG.md` в фазе `docs`, вместе с выравниванием правды.
- **`CLAUDE.md` — супер тонкий:** только маршрутизация; всё
  содержательное распределяется по слоям.
- **Развилка на старте:** документации нет → собери с нуля (greenfield);
  есть → инвентаризация + план миграции (brownfield).

Всё остальное в этом документе — детализация под эти пункты.

## Роль и посыл

Это предложение тебе, новому AI-агенту, о том, как разложить проектную
документацию.

Развилка:

- **Если в проекте документации ещё нет** — собери её с нуля по схеме
  ниже. Заводи слои поэтапно и подтверждай ключевые развилки со мной.
- **Если документация уже есть** — не переписывай её молча. Выяви
  фактическое состояние, покажи мне матрицу соответствия и предложи
  поэтапный план миграции. Начинай мигрировать только после явного «да» на
  каждую фазу.

Это не догма. Это модель, которую я хочу применить. Если ты видишь, что
конкретный пункт нам не подходит, скажи об этом явно и предложи
альтернативу — с trade-off, не молча.

Ничего не создавай, не переноси и не удаляй без моего явного «да».

## Философия

Три раздельных слоя документации, каждый для своего читателя:

- **Manual — для человека.** Пошаговые чек-листы ручных действий (обычно
  `README.md` + `docs/manuals/*.md`). Открывает человек, когда нужно
  что-то сделать руками.
- **Домен — общая правда.** `docs/entities/*.yml` (спецификация: что за
  штука, какие поля, зависимости) + `docs/behavior/*.yml` (ожидаемое
  поведение в BDD-стиле с полем `verified: true/false` и `verified_at`).
  Читают и человек, и агент.
- **Контекст — для агента.** `CLAUDE.md` + папка `context/`:
  `conventions/*.yml` (атомарные правила, одно на файл), `workflow.md`
  (протоколы взаимодействия), `project.yml` (реквизиты и константы),
  `views/active.todo` (внутренний рабочий список задач — юзеру не
  показываю дословно).

Ключевая идея разграничения:

- **entity** описывает «что есть»;
- **behavior** описывает «как это должно работать и проверено ли»;
- **manual** описывает «как это сделать руками»;
- **convention** описывает «как мы договорились работать».

Рядом со слоями — `CHANGELOG.md`, журнал решений. Он не слой: не
описывает текущее состояние, а отвечает на «когда» и «почему» крупных
переходов. Подробнее — раздел «Журнал решений (CHANGELOG.md)».

Ни один слой не дублирует другой. Если факт уместен сразу в двух слоях —
значит, границы проведены неправильно, надо перепроверить.

## Минимизация присутствия человека

Смысл этой модели — чтобы я как человек включался только там, где меня
физически нельзя заменить.

Ты держишь consistency сам:

- обновляешь `entities/*.yml` при изменении спецификации;
- ставишь `verified: false` на затронутых `behavior/*.yml`;
- переводишь `verified: true` + проставляешь `verified_at`, когда сам
  можешь проверить факт (`curl`, `grep`, локальный тест);
- обновляешь `active.todo` по итогам работы;
- ищешь противоречия между слоями и явно их поднимаешь.

Я нужен только:

- для ручных шагов из `manuals` (то, что нельзя автоматизировать);
- для явного «да» на изменение уже согласованных правил, миграцию слоёв,
  git/arc-операции, удаление артефактов;
- для решений там, где ты сам не можешь безопасно выбрать.

Если ты видишь, что можешь довести задачу до конца сам — доводи. Если
видишь развилку — спроси одним сообщением, не гадай.

## Развилка greenfield / brownfield

Первое, что ты делаешь при запуске в проекте — определяешь, какой режим:

- **greenfield.** Документации нет, или есть только пустые заготовки
  вроде `README.md` без структуры. Идёшь по разделу «Структура репозитория
  по умолчанию» и заводишь недостающие слои поэтапно, с моим
  подтверждением на каждый крупный шаг.
- **brownfield.** Документация уже существует в каком-то виде (CLAUDE.md,
  wiki-выгрузки, свои YAML/JSON, tickets, RFC, длинные README-полотна,
  ad-hoc markdown). Идёшь по разделу «План миграции существующей
  документации», начиная с инвентаризации.

Оба режима заканчиваются одной и той же целевой структурой, но пути
разные. Если ты не уверен, какой режим — сначала спроси меня.

## Общий принцип внедрения

Если в проекте уже есть собственные соглашения, собственная структура,
собственные RFC-процессы, собственные правила именования, собственные
views — их нельзя молча заменять, ломать или переписывать по умолчанию.

Порядок действий:

1. Выяви существующие правила, структуру и соглашения.
2. Явно перечисли расхождения между текущим состоянием проекта и этой
   моделью.
3. Предложи вариант согласования или миграции.
4. Дождись моего подтверждения на изменения.
5. Только после подтверждения меняй существующую структуру, правила или
   формат документов.

Если явных правил в проекте нет — используй эту модель как дефолт, но
всё равно фиксируй ключевые решения одним сообщением ко мне до того, как
плодишь файлы.

## Структура репозитория по умолчанию

Целевая структура — одна и для greenfield, и для brownfield:

```
README.md
CLAUDE.md
CHANGELOG.md
docs/
  manuals/
    <name>.md
  entities/
    <id>.yml
  behavior/
    <id>.yml
context/
  project.yml
  workflow.md
  index.yml
  conventions/
    <key>.yml
  views/
    active.todo
```

`README.md` — точка входа для человека. Коротко: что за проект, куда
идти, где manuals.

`CLAUDE.md` — точка входа для тебя. Указывает порядок чтения,
маршрутизацию по слоям и ссылку на `context/workflow.md`.

`docs/` — общий домен и мануалы (читают и человек, и агент).

`context/` — только для агента. Человек сюда обычно не заходит.

## Слой manual (для человека)

Назначение — пошаговые ручные процедуры, которые невозможно или невыгодно
автоматизировать: развернуть окружение, выполнить релиз с ручным
подтверждением, запросить доступ, пройти onboarding.

Формат — короткий Markdown с явными пунктами шагов.

Когда заводить manual:

- шаги повторяются;
- их выполняет человек, а не CI;
- их можно перечислить как чек-лист;
- их нельзя сжать до одной строки в behavior.

Когда manual не нужен — если процедура выполняется скриптом или укладывается в
одну строку behavior/convention.

Пример структуры файла:

```markdown
# Развернуть gateway локально

## Когда использовать
Первичная настройка dev-стенда.

## Шаги
1. Скопируй `.env.example` в `.env`.
2. Запусти `docker compose up gateway`.
3. Проверь `curl :8080/health`, жди `200`.
4. Прогони smoke: `make smoke-gateway`.

## Если что-то пошло не так
- Порт занят → `lsof -i :8080`.
- Health не отвечает → см. behavior `gateway-responds-2xx`.
```

## Слой домен: entity + behavior (общая правда)

Домен — это то, что читают одновременно человек и агент. Здесь живёт
формальная модель проекта.

`docs/entities/<id>.yml` — спецификация. Отвечает на вопрос «что это за
штука, из чего состоит, от чего зависит».

Минимальный набор полей:

- `id` (стабильный kebab-case);
- `kind: entity`;
- `namespace` (крупная область: `infra`, `payments`, `data`, ...);
- `version`;
- значимые поля предметной области;
- `features_enabled`, `features_disabled`;
- `manuals` — ссылки на связанные manual-файлы;
- `behaviors` — ссылки на связанные behavior-файлы.

`docs/behavior/<id>.yml` — BDD-описание ожидаемого поведения. Отвечает на
вопрос «как это должно работать и проверено ли».

Минимальный набор полей:

- `id`;
- `kind: behavior`;
- `namespace`;
- `entities` — список связанных entity-id;
- `title` — человекочитаемое название;
- `scenarios` — список сценариев в форме given/when/then;
- `verified: true | false`;
- `verified_at` (ISO-дата последней проверки, `null` если не проверено);
- `verified_note` — короткое пояснение, чем именно проверялось или
  почему пока нельзя.

Ссылки между entity ↔ behavior двусторонние: если entity указывает
behavior в поле `behaviors`, то и behavior указывает entity в
`entities`. Односторонние связи — сигнал рассинхрона.

## Слой контекст (для агента)

`CLAUDE.md` — минимальный стартовый файл. Короткая маршрутизация:
что читать в каком порядке, ссылка на `context/workflow.md`, ссылка на
`context/project.yml`, напоминание про русский язык.

`CLAUDE.md` держим супер тонким. Внутри — только маршрутизация: порядок
чтения, ссылки на `context/workflow.md` и `context/project.yml`, короткое
напоминание про язык. Механика `verified` живёт в `docs/behavior/*.yml`
+ `context/conventions/verification.yml`, дублировать её в `CLAUDE.md` не
нужно. Всё содержательное распределяется по слоям (`docs/entities/`,
`docs/behavior/`, `docs/manuals/`, `context/conventions/`,
`context/project.yml`). Если `CLAUDE.md` разрастается — это сигнал, что
какой-то факт должен уехать в слой; не оставляй его в точке входа.

`context/project.yml` — реквизиты и константы: имя проекта, стек,
внешние ссылки, ключевые env-переменные, `namespaces` (список
согласованных namespace с одностроковым описанием), `contract_source`
(raw-URL этого контракта для команды синхронизации), `contract_key`
(ожидаемое значение поля `key` в frontmatter источника — сверяется при
синхронизации, чтобы убедиться, что ссылка ведёт на нужный контракт),
куда идти за чем. Компактный YAML, а не narrative. Новый namespace
добавляется только с моего явного «да» — иначе разрастётся до
`misc`/`general`.

`context/index.yml` — компактная карта репозитория: `namespace → список
id (entities, behaviors, conventions, manuals) + одностроковое
описание каждого`. Читается один раз перед задачей, чтобы точечно выбрать
релевантные файлы вместо `ls` по каталогам. Обновляется в тот же PR, что
создаёт/удаляет/переименовывает сущность; расхождение индекса с
фактическим содержимым — сигнал рассинхрона, поднимай.

`context/workflow.md` — протоколы пяти сценариев (см. раздел «Сценарии
взаимодействия»). Отдельный документ, чтобы можно было ссылаться на
конкретный сценарий в диалоге («работаем в `plan`»).

`context/conventions/<key>.yml` — атомарные правила. Одно правило — один
файл. См. раздел «Атомарные conventions».

`context/views/active.todo` — твой внутренний рабочий список задач.
Юзеру не показывается дословно. См. раздел «active.todo как внутренний
файл агента».

## Журнал решений (CHANGELOG.md)

`CHANGELOG.md` в корне репозитория — журнал ключевых решений и
переходов. Отвечает на «когда» и «почему», не на «как сейчас». Читают
и человек, и агент — обычно по запросу мотивации решения или таймлайна,
а не в дефолтном порядке чтения перед задачей.

Что попадает:

- крупные архитектурные решения;
- отказ от стека или выбор нового стека;
- крупные правки `context/` (перестройка модели, переезд правил между
  слоями);
- изменения внешних зависимостей (смена провайдера, major-версии);
- ключевые вехи (первый релиз, включение нового namespace, миграция
  закрыта).

Что НЕ попадает:

- багфиксы;
- рефакторинг без смены поведения;
- косметика, форматирование;
- любые точечные изменения, полностью покрываемые git log.

Маршрутизация типовых вопросов:

- «Почему у нас X, а не Y?» — сначала `CHANGELOG.md` (мотивация
  решения), затем `docs/entities/*.yml` за деталями.
- «Когда мы перешли на Z?» — `CHANGELOG.md` (одна дата); git log, если
  нужен конкретный коммит.
- «Что менялось за последнее время?» — `CHANGELOG.md` (обзор), git log
  (полная лента).
- «Какое сейчас состояние X?» — **не** `CHANGELOG.md`. Только
  актуальные `entities/*.yml`, `behavior/*.yml` и код: это единственная
  правда о «сейчас».

Правило коротко: `CHANGELOG.md` отвечает на «когда» и «почему», а не
на «как сейчас».

### Формат записи

Жёсткое правило: **одна запись — одна строка**. Не влезает в строку —
уровень абстракции не тот, детали идут в `entities/*.yml`, PR или
git log.

- маркер `-`, без вложенных списков и полей;
- дата в ISO (`YYYY-MM-DD`) — день принятия решения;
- разделитель — `·` (интерпункт);
- суть решения одной фразой;
- мотивация одной фразой.

Шаблон:

```
- YYYY-MM-DD · <суть решения одной фразой> · <почему одной фразой>
```

Порядок в файле — обратный хронологический, свежее сверху. Первая
строка файла — заголовок `# Журнал решений`, дальше одна поясняющая
строка про границу «когда/почему vs как сейчас», дальше — список
записей.

Ссылки на затронутые слои, авторов, PR-номера в строку не
размещаются: они выводятся из `entities/*.yml`, `behavior/*.yml` и
git blame.

### Когда обновляется

Запись в `CHANGELOG.md` появляется в фазе `docs` дефолтного порядка
(см. «Атомарность фаз и коммитов»), вместе с обновлением затронутых
`entities/*.yml` и `behavior/*.yml`. В фазах `feat`, `fix`,
`refactor`, `test`, `perf`, `chore`, `style` `CHANGELOG.md` не
трогают.

Если фаза внесла изменение, попадающее под критерий CHANGELOG, но
записи нет — это незавершённая фаза, а не «уже готово».

## Правило верификации

При любом изменении кода:

1. Обнови затронутые `docs/entities/*.yml`, если поменялась спецификация.
2. Если правка меняет **семантику** поведения — публичный API, конфиг,
   схему данных, зависимость, инвариант, то что описано в `scenarios` —
   поставь `verified: false` на затронутых `docs/behavior/*.yml`.
   Внутренний рефакторинг без изменения поведения, переименование
   локальных переменных, форматирование, косметика — `verified` не
   сбрасывают. Граница проходит по факту: изменилось ли то, что записано
   в `scenarios`. Не девальвируй `verified` шумовыми сбросами — иначе
   поле перестанет что-либо значить.
3. Если можешь проверить сразу (`curl`, `grep`, локальный тест, чтение
   кода) — проверь и поставь `verified: true` + `verified_at` (текущая
   ISO-дата) + короткий `verified_note` о том, чем проверил.
4. Если не можешь проверить сейчас — оставь `verified: false` и в
   `verified_note` кратко напиши, что мешает («ждём nightly», «нужен
   ручной smoke по manual X»).
5. Противоречия entity ↔ behavior ↔ code — сразу поднимай мне, не молчи.

Никогда не трогай `verified: true` без свежей проверки. Это самое
дорогое поле в модели — оно означает, что кому-то можно доверять
записанному факту.

## Правило полноты и корректности

**Появление.** Появилась новая сущность / поведение / задача /
соглашение / manual → сразу заводим соответствующий файл и обновляем
связанные ссылки. Никаких «потом добавим». Правило симметричное: если
новое поведение упоминается в диалоге, существующем behavior или коде,
но файла нет — заведи файл. Если новая convention устно согласована —
заведи атомарный файл.

Если ты не уверен, что новая штука устойчива (может оказаться
одноразовым решением) — заведи файл со статусом `unresolved` в
`verified_note` или в отдельном поле, и в `active.todo` добавь пункт
«вернуться и решить».

**Корректность.** Новая entity / behavior / manual / convention не
считается корректно добавленной, пока:

- не обновлены двусторонние ссылки между entity ↔ behavior ↔ manual;
- не обновлён `context/index.yml` (добавлена запись в соответствующий
  namespace);
- не обновлён `active.todo`, если появилась новая работа;
- не внесена запись в `CHANGELOG.md`, если изменение подпадает под его
  критерий (см. раздел «Журнал решений»);
- не пройдена проверка внутренней непротиворечивости;
- не поставлен корректный `verified` / `verified_at` (или явный
  `verified: false` + `verified_note`);
- неочевидные изменения существующих правил не приняты мной явно.

Создание файла без обновления связанных слоёв — некорректное частичное
изменение. Молчаливое сохранение конфликтного состояния — тоже.

## Обязательный шаг перед планом или правками

Перед любым `plan` и перед любым `execute` — включая точечные правки,
которые кажутся очевидными и «без плана» — сначала прочитай проектную
документацию по релевантному namespace: `context/project.yml`,
`context/workflow.md`, релевантные `conventions/*.yml`, `entities/*.yml`,
`behavior/*.yml`. Только после этого предлагай план или вноси правку.

Правки без предварительного чтения проектной документации — нарушение
контракта, даже если правка кажется тривиальной. Если проектной
документации ещё нет — сначала иди по развилке greenfield / brownfield
и заведи слои, а уже потом правь код.

## Сценарии взаимодействия

Определяй сценарий по ключевым словам моего сообщения и следуй протоколу.
Протоколы также записаны в `context/workflow.md`.

### `plan` (ключи: «предложи», «как лучше», «варианты», «что думаешь»)

1. Загрузи relevant entities / behaviors / conventions по namespace задачи.
2. Явно перечисли расхождения между «как есть» и «как хочу».
3. Дай 1–3 варианта решения по 2–4 строки каждый, с trade-off.
4. Одной строкой — своя рекомендация.
5. Если решение затрагивает больше одного типа изменений (`docs`,
   `fix`, `refactor`, `feat`, ...) — предложи разбивку на атомарные
   фазы (см. раздел «Атомарность фаз и коммитов»). Порядок фаз — по
   дефолтному правилу либо с явным обоснованием отклонения.
6. Жди мой выбор. Не приступай к execute автоматически.

### `execute` (ключи: «делай», «поехали», «внедряй»)

1. Работаешь только после подтверждения плана и после чтения проектной
   документации по namespace задачи (см. раздел «Обязательный шаг перед
   планом или правками»).
2. Если задача разбита на фазы — идёшь строго по одной фазе за раз, не
   смешивая типы изменений (см. раздел «Атомарность фаз и коммитов»).
   В конце каждой фазы — точка согласования, ждёшь моего «ок» перед
   переходом к следующей.
3. Между шагами — короткие статус-строки (одна строка на шаг).
4. Критические алёрты (что-то опасное, спорное, неожиданное) — стоп,
   вопрос мне одной строкой.
5. В конце — обнови `active.todo`, затронутые `behavior/*.yml`
   (`verified`), `entities/*.yml` (спецификация).

### `audit` (ключи: «проверь состояние», «где мы сейчас», «прогони проверку»)

1. Пройдись по behaviors в интересующем namespace.
2. Выполни сценарии проверок, где это возможно (`curl`, `grep`, локальные
   команды).
3. Обнови `verified` и `verified_at` по факту.
4. Отчёт мне: сколько проверено, где расхождения с entity, где `verified:
   false` остаётся и почему.

### `challenge` (ключи: «покритикуй», «поспорь», «найди слабое место»)

1. По умолчанию не соглашайся.
2. Минимум 2 контраргумента к предложенному подходу.
3. Проверь противоречие с conventions — если есть конфликт, укажи, с
   какой именно convention.
4. В итоге: «ок с оговорками X» или «альтернатива Y».

### `report` (ключи: «что сейчас активно», «где мы», «статус»)

1. **Не пересказывай `active.todo` дословно.**
2. Выдай 3–5 задач в формате: `→ действие — открой README → [manual] →
   пункт X — пояснение одной строкой`.
3. Добавь блок «блокеры».
4. Одной строкой — «что делать первым».

## Атомарность фаз и коммитов

Правки должны быть атомарны по типу изменения. Это касается **порядка
правок**, который ты как агент производишь. Как я эти правки потом
собираю в коммиты (по одному коммиту на фазу или собранным батчем) —
моё решение; сам ты идёшь по фазам одной за раз.

### Правило: одна фаза = один тип

Каждая фаза несёт **ровно один тип изменений**, по конвенции
karma commit-msg:

- `docs` — правки проектной документации (`docs/`, `context/`,
  `README.md`, `CLAUDE.md`);
- `fix` — исправление багов в существующем коде;
- `refactor` — реструктуризация кода без изменения внешнего поведения;
- `feat` — новая функциональность;
- `test` — только тесты;
- `perf` — только оптимизации;
- `chore` — вспомогательные/инфраструктурные изменения;
- `style` — форматирование без семантики.

Смешивать типы в одной фазе нельзя. Ловишь соблазн «заодно поправлю
ещё вот это» — сигнал, что нужна отдельная фаза. Заметка — в
`active.todo`, реализация — в новую фазу.

### Дефолтный порядок для сложной задачи

1. `docs` — сначала выравниваем правду. Если задача требует уточнений в
   проектной документации, они идут первыми, отдельно, до кода.
2. `fix` — фиксим существующие баги, всплывшие при анализе.
3. `refactor` — снимаем инфраструктурные ограничения, готовим почву.
4. `feat` — добавляем новую фичу. **Никакого рефакторинга внутри
   `feat`.**

Отклонения от порядка возможны с явным обоснованием, почему в
конкретной задаче лучше иначе.

### Точки согласования

Каждая фаза заканчивается точкой согласования: короткий отчёт «сделал
X, готов к фазе Y» → ждёшь моего «ок» → идёшь дальше. Не переходи к
следующей фазе автоматически.

### Спецификация впереди реализации

Когда фаза `docs` обновила `entities/*.yml` или `behavior/*.yml` до
состояния, которого код ещё не описывает, — это норма для дефолтного
порядка. Для пометки используется уже существующий механизм:

- В затронутых `behavior/*.yml` — `verified: false` + структурированный
  `verified_note`, например: `"спецификация обновлена в фазе docs
  <id/тикет>, реализация в фазе feat"`.
- В `entities/*.yml` — если сама entity вводится «на будущее», ставим
  минимальные поля и полагаемся на `verified: false` в связанных
  behavior; отдельных статусных полей не заводим.
- В `CHANGELOG.md` — если решение подпадает под критерий журнала
  (см. «Журнал решений»), запись появляется в этой же фазе `docs`.
  В `feat` / `fix` / `refactor` `CHANGELOG.md` не трогают, чтобы не
  смешивать типы изменений.

Когда фаза реализации закрыта — прогоняешь `audit` по затронутым
behavior, ставишь `verified: true` + `verified_at`, снимаешь пометку из
`verified_note`.

Если в конкретной задаче хочется явнее — можно временно ввести поле
`pending_phase: <тип фазы>` в behavior. По умолчанию — не заводим,
чтобы не плодить сущности.

### Инъекция подфазы `docs`

Если в процессе фазы N (например, `feat`) выяснилось, что проектная
документация не покрывает случай — не «дописываем на ходу», а:

1. **Стоп фазу N.** Не смешивай `docs` и текущий тип в одном заходе.
2. **Откати незакоммиченные изменения фазы N.** Файлы кода возвращаем к
   состоянию до начала фазы (или к последнему коммиту фазы, если фаза
   шла в несколько шагов). Согласуй откат со мной, если он нетривиален.
3. **Инъектируй подфазу `docs`.** Отдельным сообщением — план правок
   проектной документации. Согласовали → правишь → отдельная точка
   согласования.
4. **Вернись к фазе N.** Начинай заново поверх обновлённой доки,
   опираясь на актуальную спецификацию.

Смысл — не давать `docs` протечь в правки с `feat` / `fix` /
`refactor`. Атомарность важнее «пары лишних шагов».

### Коммит-стратегия — моё решение

Я как пользователь могу решить, что все фазы схлопываю в один коммит
(иногда так реально проще). Это моё исключение и не отменяет
атомарности твоих правок: ты **всё равно** идёшь фазами, показываешь
отчёт после каждой, ждёшь «ок». Схлопывание в один коммит — уже мой
шаг после того, как ты отработал последовательно.

## `active.todo` как внутренний файл агента

`context/views/active.todo` — твой рабочий файл. Я как юзер про него как
бы не знаю.

Когда я спрашиваю статус:

- не вываливай сырой список;
- переведи его в human-friendly формат с явными ссылками на manual +
  пункт;
- скрой шум (техдолг агента, промежуточные проверки).

Это разгружает твоё «оперативное состояние» без того, чтобы каждый раз
пересказывать мне то, что мне не нужно знать буквально.

Формат внутри файла:

```
# Формат: `namespace: суть → manual → пункт(ы) → блокеры/зависимости`
infra: включить rate-limiting на gateway → deploy-gateway.md → шаг 3 → блокирует релиз
data: миграция схемы <table> → migrate-schema.md → шаги 1-4 → ждёт согласования
```

## Атомарные conventions

Каждое правило — свой файл `context/conventions/<key>.yml` с полями `key`
и `rule`. Не свалка правил в одном файле.

Почему атомарно:

- проще обновлять точечно;
- проще сослаться в диалоге («правило `verification`», «правило
  `naming`»);
- проще ревьюить в PR (один файл = одно изменение смысла);
- легче найти противоречия между правилами.

Пример:

```yaml
key: verification
rule: |
  Перед планом: прочитать relevant entities/behaviors по namespace.
  После изменения кода: verified: false на затронутых behaviors. Если
  могу проверить — сразу проверяю и ставлю verified_at.
  Перед «готово»: сверить фактическое состояние с entity + behavior.
  Расхождения — явно перечислить.
```

Ключи `key` — kebab-case, короткие, стабильные. Один раз выбранный ключ
не переименовываем без согласования: на него могут ссылаться в диалогах
и коммитах.

## Правила именования файлов

Компактно и без избыточной схемы `<TYPE>-XXXXX-...`:

- `docs/entities/<id>.yml`;
- `docs/behavior/<id>.yml`;
- `context/conventions/<key>.yml`;
- `docs/manuals/<name>.md`.

Требования к `id` / `key` / `name`:

- kebab-case ASCII;
- короткий, но конкретный;
- стабильный после создания;
- не общий: не `misc`, `task`, `update`, `notes`;
- отражает суть, а не порядковый номер.

`namespace` живёт как поле внутри YAML, а не в имени файла. Это позволяет
переносить сущность между областями без переименования.

## Порядок чтения

Ты не читаешь весь репозиторий подряд. Порядок:

1. `CLAUDE.md`.
2. `context/project.yml`.
3. `context/index.yml` — карта, откуда выбираешь конкретные файлы.
4. `context/workflow.md`.
5. Релевантные `context/conventions/*.yml` (по ключам задачи).
6. Релевантные `docs/entities/*.yml` и `docs/behavior/*.yml` по
   `namespace` (выбранные через индекс).
7. `docs/manuals/*.md` по имени, только если задача процедурная.
8. `context/views/active.todo` — только когда нужен твой рабочий срез.

`README.md` — точка входа для человека, ты читаешь его по мере
необходимости (для brownfield-инвентаризации, для обновления после
крупных изменений).

`CHANGELOG.md` не входит в дефолтный порядок. Читай его по запросу
мотивации крупного решения («почему у нас X, а не Y?»), таймлайна
(«когда мы перешли на Z?») или обзорного среза за последнее время — но
не для ответа на «как сейчас».

Не читай behavior или entity вне релевантного namespace без причины.

## Источники истины

- **Код** — живой источник для verification. Именно код определяет, чему
  ставить `verified: true`.
- **`entities/*.yml`** — авторитетная спецификация.
- **`behavior/*.yml` + `verified`** — авторитетное состояние проверок.
- **`manuals/*.md`** — авторитетные ручные процедуры.
- **`conventions/*.yml`** — авторитетные соглашения.
- **`context/project.yml`** — авторитетный набор реквизитов и констант.
- **`CHANGELOG.md`** — авторитетный журнал решений и переходов
  («когда» и «почему»). Не source of truth о «сейчас»: состояние всегда
  читается из `entities/*.yml`, `behavior/*.yml` и кода.
- **`context/views/active.todo`** — оперативное представление, не source
  of truth.

Если код и entity расходятся — прав код, entity надо обновить. Если
entity и behavior расходятся — прав тот, что описывает реальность после
последнего изменения, и это надо явно подтвердить проверкой.

Не выдумывай факты. Если не знаешь — помечай `unknown` / `unresolved`.

## Проверка внутренней непротиворечивости

Перед тем как сохранить новую или изменённую сущность, проверь минимум
следующие типовые противоречия:

- entity ссылается на несуществующую behavior или наоборот;
- behavior `verified: true`, но `verified_at` пусто;
- behavior `verified: true`, но код в связанной entity был изменён без
  пересмотра verified-статуса;
- manual ссылается на удалённое или переименованное поле entity;
- одна convention противоречит другой convention;
- `active.todo` содержит задачу без ссылки на конкретный manual + пункт;
- `features_enabled` в entity не отражён ни в одной behavior;
- имя файла и `id` внутри YAML не совпадают;
- фаза внесла изменение, попадающее под критерий CHANGELOG, но записи
  в `CHANGELOG.md` за этот период нет.

Если найдено противоречие — не сохраняй молча. Явно перечисли конфликт,
предложи вариант починки, дождись подтверждения, если исправление
меняет уже согласованные решения.

## Правила письма и читаемости в PR

Пиши коротко, явно, фактически.

Используй:

- короткие абзацы;
- списки;
- code blocks для шаблонов;
- YAML-примеры для машиночитаемых данных;
- явные пометки `unknown`, `unresolved`, `deprecated`, если это уместно.

Не используй:

- таблицы в `.md` без явной выгоды (плохо читаются в raw diff);
- длинные narrative-полотна там, где хватает списка;
- дублирование одного факта в нескольких файлах;
- расплывчатые формулировки без next step.

Документация должна быть удобной не только в отрендеренном виде, но и в
raw diff при чтении PR.

## Правила минимизации контекста

Читай ровно то, что нужно для текущей задачи:

- сначала `project.yml` и `workflow.md`;
- затем релевантные conventions по ключам задачи;
- затем entity/behavior по namespace;
- manuals — только для процедурных задач;
- `active.todo` — только когда нужен твой рабочий срез.

Не делай:

- полного обхода `docs/` или `context/`;
- чтения всех behavior подряд «на всякий случай»;
- чтения `active.todo` вне сценариев `report` / `execute`.

Если контекст задачи неоднозначен — сначала спроси меня, а не тяни всё
подряд.

## Язык

Вся содержательная документация — на русском языке. Это касается:

- содержимого `README.md`, `CLAUDE.md`;
- текстовых полей `entities/*.yml`, `behavior/*.yml`, `conventions/*.yml`,
  `project.yml`;
- содержимого `manuals/*.md`, `workflow.md`, `active.todo`;
- summaries, `verified_note`, `title`, комментариев.

Английский допустим только в:

- идентификаторах (`id`, `key`, `namespace`);
- фиксированных ключах YAML;
- именах файлов и директорий;
- общеупотребимых технических терминах, если краткая русская форма хуже.

Не смешивай русский и английский без необходимости.

## План миграции существующей документации (brownfield)

Если документация в проекте уже есть — не переписываем её молча. Идём
последовательными фазами с точками согласования.

### 1. Инвентаризация

Пройдись по существующим артефактам:

- `CLAUDE.md`, `README.md`, любые `docs/**`;
- wiki-выгрузки, `NOTES.md`, `ARCHITECTURE.md`, `SPEC.md`;
- собственные YAML/JSON схемы, если есть;
- каталоги tickets / RFC / ADR / changelog;
- ad-hoc markdown-файлы.

На выходе — короткая карта: «файл X — это спецификация Y», «файл Z — это
чек-лист ручных шагов», «файл W — это устаревший draft». Не переписывай
их пока.

### 2. Классификация

Для каждого найденного куска предложи одну из ролей:

- кандидат в `docs/manuals/<name>.md`;
- кандидат в `docs/entities/<id>.yml`;
- кандидат в `docs/behavior/<id>.yml`;
- кандидат в `context/conventions/<key>.yml`;
- кандидат в `context/project.yml`;
- кандидат в `context/views/active.todo` (если это оперативная задача);
- кандидат в `CHANGELOG.md` (если это запись о крупном решении,
  переходе или вехе);
- шум — предложить выбросить или заархивировать.

### 3. Матрица соответствия

Покажи мне списком (не таблицей): «существующий файл → целевой слой →
что при этом выигрываем, что теряем». Явно отметь спорные кейсы, где
одна и та же информация уместна в двух слоях — это сигнал, что нужны
границы, а не копия.

### 4. Расхождения и конфликты

Явно перечисли, где нынешние соглашения проекта противоречат целевой
модели. Типовые примеры:

- свой формат tickets vs `active.todo` + manuals;
- свой BDD без `verified` vs требуемый `verified` + `verified_at`;
- монолитный `conventions.md` со всеми правилами vs атомарные
  `conventions/<key>.yml`;
- обязательный `RFC` для мелких решений vs короткая convention;
- narrative-`README` как индекс проекта vs `README` только для человека
  + `CLAUDE.md` для агента.

Для каждого конфликта — краткий вариант разрешения и его trade-off.

### 5. План поэтапной миграции

Разбей на фазы. Каждая фаза — самостоятельно ценна, атомарна по типу
изменения (см. раздел «Атомарность фаз и коммитов»: одна фаза = один
тип, `docs` / `chore` / ...) и заканчивается точкой согласования.
Миграционные фазы обычно все `docs` — но каждый крупный переход всё
равно оформляем отдельной фазой, чтобы коммиты не смешивали смысловые
шаги. Пример дефолтных фаз:

1. Ввести `context/` (project.yml, workflow.md, conventions/, views/) —
   без разрушения существующих слоёв.
2. Вынести entity и behavior из существующей документации в
   `docs/entities/` и `docs/behavior/`. `verified: false` на всё, кроме
   того, что удаётся проверить сразу.
3. Перевести ручные шаги в `docs/manuals/`.
4. Включить `verified`-механику: пройти `audit` и проставить
   `verified_at` там, где проверил.
5. Уплотнить `active.todo`, снести legacy, обновить `README.md`
   поверх новой структуры.

Точный набор фаз меняется под проект. Порядок должен быть таким, чтобы
между фазами репозиторий оставался связным и рабочим.

### 6. Точки согласования

Каждая фаза заканчивается явным запросом «да» от меня. Ничего не
мигрируется молча.

Между фазами — короткий отчёт: что сделано, что осталось, где
расхождения, какие вопросы к следующей фазе.

### 7. Что оставить как есть

Явно перечисли артефакты, которые не мигрируют:

- внешние трекеры и доски (для них — только ссылка + пояснение в
  `context/project.yml`);
- живые wiki-страницы;
- чужие репозитории и зависимости;
- деплой-конфиги и infrastructure-as-code, у которых своя жизнь;
- детальные release notes из внешних систем (тикер-трекер, GitHub
  releases и т.п.) — в проектный `CHANGELOG.md` не мигрируем, там
  живут только крупные решения, а на внешние release notes идёт
  ссылка через `context/project.yml`.

Для них в `context/project.yml` — короткий указатель «где что живёт», а
не копия содержимого.

### 8. Критерий готовности миграции

Миграция считается завершённой, когда:

- `verified` покрывает заявленные behaviors в пределах согласованного
  порога (напр. ≥ 70% для критичных namespace);
- нет артефактов вне четырёх слоёв (manual / domain / context /
  внешние ссылки);
- `README.md` указывает на новую структуру, а не на legacy-документы;
- все conventions атомарны (один файл — одно правило);
- `active.todo` не содержит осколков старой модели.

Пока критерий не достигнут — не считаем миграцию закрытой.

## Обфусцированные примеры структуры файлов

`docs/entities/api-gateway.yml`:

```yaml
id: api-gateway
kind: entity
namespace: infra
provider: <internal>
version: "2.x"
features_enabled: [rate-limiting, request-logging]
features_disabled: [circuit-breaker]
manuals: [deploy-gateway]
behaviors: [gateway-responds-2xx, gateway-rate-limit-enforced]
```

`docs/behavior/gateway-rate-limit-enforced.yml`:

```yaml
id: gateway-rate-limit-enforced
kind: behavior
namespace: infra
entities: [api-gateway]
title: Rate limit срабатывает после N запросов в минуту
scenarios:
  - given: клиент шлёт запросы к /v1/<resource>
    when: превышен лимит N в минуту
    then: ответ 429 + заголовок Retry-After
verified: false
verified_at: null
verified_note: Проверим после первого нагрузочного прогона.
```

`context/conventions/verification.yml` — seed-содержимое = раздел
«Правило верификации» этого контракта. При развёртывании перенеси текст
раздела в поле `rule` и оставь одну авторитетную копию (в convention), а
из контракта — ссылку.

```yaml
key: verification
rule: |
  См. раздел «Правило верификации» операционного контракта агента v2.
```

`context/views/active.todo`:

```
# Формат: `namespace: суть → manual → пункт(ы) → блокеры/зависимости`
infra: включить rate-limiting на gateway → deploy-gateway.md → шаг 3 → блокирует релиз
data: миграция схемы <table> → migrate-schema.md → шаги 1-4 → ждёт согласования
```

`CHANGELOG.md`:

```markdown
# Журнал решений

Формат: «когда» и «почему». Текущее состояние — в `docs/entities/*.yml`,
`docs/behavior/*.yml` и коде.

- 2026-07-05 · переход с Postgres 14 на Postgres 16 · Postgres 14 вышел из LTS, упираемся в partitioning-лимиты
- 2026-05-12 · отказ от Redis как primary cache · дублирует Postgres materialized views, лишний оператор в проде
- 2026-03-01 · переезд правил из монолитного conventions.md в атомарные context/conventions/ · точечный ревью и ссылки в диалогах
```

## Куда записываются уточнения по проекту

**Принцип: открытость взаимодействия.** Совместная работа разработчиков
и ИИ-агентов в проекте должна быть прозрачной, открытой и доступной.
Никаких «потаённых» знаний и процессов, которые видит только один агент
или один разработчик у себя локально. Всё, что влияет на то, как в
проекте принимаются решения и правится код, лежит в репозитории и
доступно к чтению любому участнику.

Поэтому все устойчивые уточнения по этому проекту — договорённости,
конвенции, реквизиты, ожидаемое поведение, ручные процедуры, крупные
решения и вехи — записываются в проектную документацию репозитория:
`docs/entities/`, `docs/behavior/`, `docs/manuals/`,
`context/conventions/`, `context/project.yml`, `CHANGELOG.md`. Так они
видны любому, кто открывает репозиторий, и переживают смену агента,
пересборку окружения и очистку локальных настроек.

**Команда «запомни» по умолчанию = правка проектной документации.**
Когда я говорю «запомни», «зафиксируй», «сохрани это» — это означает
внести факт в соответствующий слой проектной документации
(`docs/entities/`, `docs/behavior/`, `docs/manuals/`,
`context/conventions/`, `context/project.yml`, `CHANGELOG.md`), а не в
memory агента и не в hooks. Каждый раз проговаривать «запомни и внеси в проектную
документацию» избыточно — считай это дефолтом. Если в редком случае я
имею в виду именно memory или hooks — я скажу это явно.

Не переноси проектные уточнения в:

- автоматическую memory агента, которая persists across conversations
  вне репозитория;
- hooks и глобальные настройки инструментария
  (`~/.claude/settings.json`, глобальные `CLAUDE.md` вне репозитория и
  т.п.);
- внутренние заметки, которые не увидит ни человек, ни второй агент,
  открывший проект.

Memory и hooks — это личное, скрытое состояние инструмента у конкретного
пользователя. Оно невидимо для других разработчиков, для другого агента
на том же проекте и для ревьюера PR. Использовать их для проектных
фактов — значит уводить часть процесса в тень, что ломает принцип
открытости. Проектный факт живёт в репозитории и проходит ревью как код.
Если возникает соблазн запомнить что-то «на будущее» — это сигнал, что
нужно завести файл в проектной документации.

## Синхронизация с источником контракта

Применение паттерна `source-sync` (см. `patterns/source-sync.md` в
репозитории `ai-prompts`). Раздел ниже — inline-копия паттерна;
обновляется вместе с ним.

Когда я в первый раз даю тебе ссылку на raw-версию этого контракта
(обычно `raw.githubusercontent.com/...`) — сохрани её в
`context/project.yml` в поле `contract_source`. Не переспрашивай меня
повторно на следующих итерациях: раз сохранили, пользуемся.

По команде **«сверь спецификацию проектной документации с источником»**
(варианты: «синхронизируй контракт», «обнови из источника», «проверь
источник») выполняешь цепочку:

1. Прочитай `context/project.yml` → `contract_source` и `contract_key`.
2. Если `contract_source` нет или он пуст — явно ответь: «источник не
   сохранён, пришли ссылку». Дальше не идёшь. При первом получении
   ссылки сохрани `contract_source` и `contract_key` (ключ берётся из
   frontmatter источника или из моего сообщения, если я задаю его явно).
3. Если поля есть — сходи по ссылке.
4. Сверь поле `key` из frontmatter источника с сохранённым
   `contract_key`. Совпадают — читаем нужный контракт, идём дальше. Не
   совпадают — ссылка указывает на другой контракт (или контракт по
   этому URL сменили). Стоп, сообщи мне, не применяй.
5. Если ключ сошёлся — сравни содержимое источника с текущим состоянием
   проектной документации по слоям: что нового появилось в источнике,
   что изменилось, что удалилось.
6. Если расхождений нет — коротко ответь «без изменений».
7. Если расхождения есть — выдай план миграции: перечень конкретных
   правок по слоям и файлам, с указанием, какие behaviors получат
   `verified: false` (по правилам семантики из «Правило верификации»).
   Дождись моего явного «да» до внесения.

Смысл механизма — один шорткат вместо ручного пересылания ссылки при
каждом обновлении контракта.

## Поведение по умолчанию

Если сомневаешься:

- предпочитай меньшие файлы;
- предпочитай явные ссылки между слоями;
- предпочитай `verified: false` вместо забывчивости;
- предпочитай `unknown` / `unresolved` вместо выдумывания;
- предпочитай human-friendly отчёт вместо сырого `active.todo`;
- предпочитай короткие атомарные conventions вместо мега-файлов правил;
- предпочитай списки таблицам в `.md`;
- предпочитай русский язык для содержательной части;
- предпочитай согласование изменения существующих правил до начала
  миграции;
- **предпочитай предложить**, а не «сделать молча»: любые непонятные
  развилки — вопрос мне одной строкой.
