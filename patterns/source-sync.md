---
key: source-sync
kind: pattern
title: Синхронизация промпта с источником через индекс + SHA-256 checkpoint
use_case: Промпт (контракт, модуль) может обновляться. Проект-потребитель хранит ссылку на индекс источника + SHA-checkpoint, одной командой сверяется с индексом и получает точечный план миграции только по разошедшимся источникам.
---

# source-sync

Механизм синхронизации промпта с его источником. Позволяет
проекту-потребителю одной командой сверить свою локальную версию (или
проектную документацию, построенную по промпту) с актуальной версией
промпта в источнике, получить план изменений и внести их с явного «да».

Поддерживает **множественные источники** (базовый контракт + подключённые
модули) через общий индекс `prompts/index.yml` с SHA-256 checkpoint по
каждому источнику: анализируются только те, у которых SHA разошёлся.

## Назначение

- Промпт эволюционирует. Проекты, которые его применили, должны иметь
  дешёвый способ узнать об обновлениях и мигрировать.
- Ссылка на raw-версию может быть подменена или указывать на другой
  промпт — сверка по стабильному ключу защищает от этого.
- Fetch по каждому источнику затратен по токенам. Единый индекс +
  SHA-checkpoint позволяет одной операцией понять, что вообще менялось,
  и точечно тянуть только разошедшееся.

## Требования к целевому промпту

В frontmatter обязательны:

- `key` — стабильный kebab-case идентификатор. Один раз выбран — не
  переименовываем без явного согласования.
- `kind` — тип промпта (`contract` / `module` / `pattern` / `prompt`).
- `patterns: [..., source-sync, ...]` — декларация применённого
  паттерна.

Для модулей (`kind: module`) дополнительно обязателен:

- `applicable_to: [<base-key>, ...]` — список ключей базовых промптов, к
  которым модуль применим.

В теле промпта — inline-блок из этого паттерна (см. ниже).

## Требования к источнику (репозитория промптов)

Источник промптов (например, `ai-prompts`) содержит единый индекс:

- **`prompts/index.yml`** — машиногенерируемый каталог всех промптов.
  Для каждого промпта — `key`, `path`, `raw` (URL), `sha256`
  (SHA-256 от файла), `applicable_to` (для модулей).
- Индекс генерируется скриптом, не пишется руками. Правда — в самих
  файлах промптов; индекс — навигация и SHA-справка.
- Расхождение индекса с фактическим содержимым файлов должно ловиться
  на CI и валить билд.

## Требования к проекту-потребителю

Проект, применяющий промпты, хранит в `context/project.yml` блок
`sync`:

```yaml
sync:
  index_source: <raw-URL prompts/index.yml источника>
  index_sha: <hex — SHA-256 индекса на момент последней синхронизации>
  contract:
    key: <key базового контракта>
    synced_sha: <hex — SHA-256 контракта на момент последней синхронизации>
```

Для каждого подключённого модуля — отдельный manifest
`context/modules/<key>.yml` со своими `source`, `synced_sha`,
`synced_at`, `provides`.

Первое сохранение полей — при первичной поставке промпта (агент берёт
`key` из frontmatter, `synced_sha` — из fetch'а). Далее пользоваться
без переспрашивания.

## Единый fetch-канал: только `curl`

Fetch источников для синхронизации выполняется **только `curl`** через
Bash с фиксированными флагами и заголовками:

```bash
curl -fsSL -H "Cache-Control: no-cache" -H "Pragma: no-cache" "<url>"
```

WebFetch и аналоги **запрещены** для source-sync:

- локальный кэш инструмента может подставить старый ответ;
- pre-processing (HTML → markdown, обрезка) искажает контент;
- SHA-checkpoint от такого ответа не сойдётся с SHA-256 raw-контента
  источника, и вся механика развалится.

SHA-256 считается от того же байт-потока:

```bash
curl -fsSL -H "Cache-Control: no-cache" -H "Pragma: no-cache" "<url>" | shasum -a 256 | awk '{print $1}'
```

На практике удобнее сохранить ответ во временный файл, посчитать SHA,
если не совпал с сохранённым — работать с этим же файлом, не делая
повторный fetch.

## Протокол команды синхронизации

Ключи команды: «сверь всё», «сверь спецификацию с источниками»,
«синхронизируй», «обнови из источника», «проверь источник».

По такой команде агент выполняет:

1. Читает `sync.index_source` из `context/project.yml`. Если пусто —
   отвечает «источник не сохранён, пришли ссылку», дальше не идёт. При
   первом получении — сохраняет.
2. `curl` тянет `index.yml`, считает SHA-256 полученного контента.
3. Если SHA совпал с `sync.index_sha` — отвечает «без изменений» и
   выходит. Ни контракт, ни модули не тянет: раз индекс не менялся,
   значит и содержимое промптов не менялось.
4. Не совпал — читает индекс. Для каждого подключённого источника
   (контракт из `sync.contract.key`, модули из `context/modules/*.yml`)
   находит актуальный `sha256` в индексе.
5. Сравнивает `sha256` из индекса с `synced_sha` в проекте:
   - совпал — источник не менялся, пропускаем;
   - разошёлся — `curl` тянет конкретный файл по `raw` из индекса,
     делает полный diff, готовит план миграции.
6. Дополнительные проверки по индексу:
   - подключённый модуль пропал из индекса → предупреждает пользователя;
   - `applicable_to` подключённого модуля больше не содержит `key`
     подключённой базы → предупреждает;
   - в индексе появились новые модули с подходящим `applicable_to` →
     упоминает как справку («опционально можно подключить»), не
     подключает сам.
7. Если расхождений нет — «без изменений» одной строкой.
8. Если расхождения есть — единый план миграции: перечень конкретных
   правок по слоям и файлам. Дожидается явного «да» до внесения.
9. После применения — обновляет все SHA: `sync.index_sha`,
   `sync.contract.synced_sha`, `synced_sha` в manifest'ах затронутых
   модулей, `synced_at` — на текущую ISO-дату.

## Проверка `key` в источнике

При тяге отдельного промпта (контракта или модуля) не через индекс, а
напрямую по ссылке — обязательная сверка: поле `key` из frontmatter
источника == сохранённый ключ (в `sync.contract.key` или в manifest'е
модуля). Не совпало — стоп, сообщи пользователю, не применяй. Защита от
подмены URL.

## Inline-блок для целевого промпта

Ниже — текст, который копируется в тело целевого промпта как отдельный
раздел (обычно ближе к концу, до раздела «Поведение по умолчанию» или
аналога). Секция помечается как применение паттерна `source-sync`.

---

### Синхронизация с источниками

Применение паттерна `source-sync` (см. `patterns/source-sync.md` в
репозитории `ai-prompts`). Inline-копия обновляется вместе с паттерном.

#### Единый fetch-канал: только `curl`

Fetch источников для синхронизации выполняется **только `curl`** через
Bash с фиксированными флагами и заголовками:

```bash
curl -fsSL -H "Cache-Control: no-cache" -H "Pragma: no-cache" "<url>"
```

WebFetch для source-sync **запрещён**. Причины: локальный кэш
инструмента может подставить старый ответ, pre-processing (HTML →
markdown, обрезка) искажает контент, и SHA-checkpoint от такого ответа
не сойдётся с SHA-256 raw-контента источника.

SHA-256 считается от того же байт-потока, что и содержимое для анализа:

```bash
curl -fsSL -H "Cache-Control: no-cache" -H "Pragma: no-cache" "<url>" | shasum -a 256 | awk '{print $1}'
```

На практике удобнее сохранить ответ во временный файл (`curl ... -o
$tmp`), посчитать SHA от файла (`shasum -a 256 $tmp | awk '{print $1}'`),
и если не совпал с сохранённым — работать с этим же файлом.

#### Конфигурация проекта

Проект в `context/project.yml` хранит блок `sync`:

```yaml
sync:
  index_source: <raw-URL prompts/index.yml источника>
  index_sha: <hex — SHA-256 индекса на момент последней синхронизации>
  contract:
    key: <key базового контракта>
    synced_sha: <hex — SHA-256 контракта на момент последней синхронизации>
```

Manifest каждого подключённого модуля — в отдельном
`context/modules/<key>.yml` (см. раздел «Модули расширения» контракта);
в блок `sync` проекта модули не дублируются.

Первое сохранение полей — при первичной поставке (агент берёт `key` из
frontmatter источника, `synced_sha` — из fetch'а). Далее пользоваться
без переспрашивания.

#### Команда «сверь всё»

Ключи команды: «сверь всё», «сверь спецификацию с источниками»,
«синхронизируй», «обнови из источника», «проверь источник».

Единый протокол — за один заход обходит контракт и все подключённые
модули через индекс:

1. Прочитай `sync.index_source` из `context/project.yml`. Если пусто —
   ответь «источник не сохранён, пришли ссылку», дальше не идёшь. При
   первом получении ссылки — сохрани.
2. `curl` тянет `index.yml`, считаем SHA-256.
3. Совпал с `sync.index_sha` — коротко ответь «без изменений» и
   выходи. Ни контракт, ни модули не тянем: индекс не менялся, значит
   у источника ничего не поменялось.
4. Не совпал — читай индекс. Для каждого подключённого источника
   (контракт из `sync.contract.key`, модули из `context/modules/*.yml`)
   найди актуальный `sha256` в индексе.
5. Сравни `sha256` из индекса с `synced_sha` в проекте:
   - совпал — источник не менялся, пропускаем;
   - разошёлся — `curl` тянет конкретный файл (`raw` из индекса),
     делаем полный diff, готовим план миграции.
6. Дополнительно по индексу:
   - подключённый модуль пропал из индекса → предупреди пользователя;
   - `applicable_to` подключённого модуля больше не содержит ключ
     подключённой базы → предупреди пользователя;
   - в индексе появились новые модули с подходящим `applicable_to` →
     упомяни как справку («опционально можно подключить»), не подключай
     сам.
7. Если расхождений нет — «без изменений» одной строкой.
8. Если расхождения есть — единый план миграции: перечень конкретных
   правок по слоям и файлам. Дождись явного «да» до внесения.
9. После применения обнови все SHA: `sync.index_sha`,
   `sync.contract.synced_sha`, `synced_sha` в manifest'ах затронутых
   модулей, `synced_at` — на текущую ISO-дату.

#### Проверка `key` в источнике

При тяге контракта или модуля отдельно (не через индекс) — обязательная
сверка: поле `key` из frontmatter источника == сохранённый ключ (в
`sync.contract.key` или в manifest'е модуля). Не совпало — стоп,
сообщи пользователю, не применяй. Защита от подмены URL.

#### Смысл механизма

Один шорткат вместо ручного пересылания ссылки при каждом обновлении.
SHA-checkpoint экономит токены анализа: не совпал только один из пяти
источников — читаем и анализируем только его.

---

## Примеры использования

- `prompts/productivity/agent-operational-contract-v2.md` — базовый
  контракт для развёртывания проектной документации. Применяет
  `source-sync` для собственного обновления и для синхронизации
  подключённых модулей.
- `prompts/modules/knowledge-external-gui.md` — модуль расширения
  контракта для работы с GUI внешних систем. Применяет `source-sync`
  как обязательный паттерн для всех модулей.
- `prompts/productivity/personal-project.md` — контракт личного проекта
  (человек + приложение в одной петле). Применяет `source-sync` для
  собственного обновления и синхронизации подключённых модулей.
